# Core CLI has no required third-party dependencies.

# Optional private API and typed validation contracts:
fastapi
uvicorn
pydantic

# Optional real LLM adapter:
openai

# Optional public MCP adapter:
httpx
mcp

# Development:
pytest
pytest-asyncio
